import React from "react";
import Header from "../projectHeader/Header";

const Ecommerce = () => {
  const topics = ["Inventry AI", "Text Extraction from Store Bills"];
  return (
    <div>
      <Header topics={topics} />
    </div>
  );
};

export default Ecommerce;
